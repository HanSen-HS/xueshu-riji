# 学术日志 · 使用说明

## 启动应用

```bash
cd /Users/hansen/Desktop/瀚森/学术日志
python3 app.py
```

浏览器打开：http://localhost:5001

---

## 配置 Google Drive 登录（5步）

### 第1步：创建 Google Cloud 项目
1. 打开 https://console.cloud.google.com
2. 点击顶部项目选择器 → 「新建项目」，填写项目名称，创建

### 第2步：启用必要 API
1. 左侧菜单 →「API 和服务」→「库」
2. 搜索 **Google Drive API** → 启用
3. 搜索 **People API** → 启用

### 第3步：配置 OAuth 同意屏幕
1. 「API 和服务」→「OAuth 同意屏幕」
2. 用户类型选「外部」→ 创建
3. 填写应用名称（随意），其余可默认，保存
4. 「测试用户」里添加你自己的 Gmail 地址

### 第4步：创建 OAuth 凭据
1. 「API 和服务」→「凭据」→「创建凭据」→「OAuth 客户端 ID」
2. 应用类型：**Web 应用**
3. 已获授权的重定向 URI 填写：
   ```
   http://localhost:5001/oauth2callback
   ```
4. 点击「创建」，然后下载 JSON 文件

### 第5步：放置凭据文件
将下载的 JSON 文件重命名为：
```
client_secrets.json
```
放到此目录下：
```
/Users/hansen/Desktop/瀚森/学术日志/client_secrets.json
```

重启应用后，点击「使用 Google 登录」即可授权。

---

## 功能说明

| 功能 | 说明 |
|------|------|
| 每日学习记录 | 记录学习活动、分类、时长 |
| 学习感想 | 自由书写当日思考与感悟 |
| 关键洞见 | 记录重要结论和发现 |
| Google Drive 关联 | 搜索并链接相关文档 |
| 心情追踪 | 5档心情记录 |
| 标签系统 | 按关键词分类整理 |
| 打卡日历 | 可视化学习连续性 |
| 统计面板 | 累计时长、连续天数等 |
